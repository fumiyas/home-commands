#!/bin/ksh

for f in "$@"; do
  if [[ -z "${f##-*}" ]]; then
    echo "$f"
    continue
  fi
  while :; do
    echo "${f:-/}"
    d="${f%/*}"
    [[ "$d" = "$f" ]] && break
    f="$d"
  done
done \
|xargs ls -d

